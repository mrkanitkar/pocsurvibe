# ── Xcode ────────────────────────────────────────────────────────────
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/
xcuserdata/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
*.xcresult
*.xccheckout

# ── macOS ────────────────────────────────────────────────────────────
.DS_Store
._*
*.swp
*~
.Spotlight-V100
.Trashes

# ── Swift Package Manager ────────────────────────────────────────────
.build/
Packages/*/.build/
Packages/*/Package.resolved
Package.pins
.swiftpm/

# ── CocoaPods (not used, but guard against accidental inclusion) ─────
Pods/

# ── CI Scripts ───────────────────────────────────────────────────────
ci_scripts/*.log

# ── Linting & Formatting backups ─────────────────────────────────────
.swiftlint.yml.bak
.swift-format.bak

# ── Docs — architecture, sprint specs, app data exports, reviews ─────
docs/

# ── Claude Code / AI — machine-specific config and plans ─────────────
.mcp.json
.claude/plans/
.claude/launch.json
.claude/serve-missinglink.py
.claude/scheduled_tasks.lock
.claude/worktrees/
.superpowers/
.xcodebuildmcp/
PLAN.md
SurVibe_Code_Review.md
llm.txt

# ── Secrets — NEVER commit credentials, keys, or certificates ───────
.env*
Secrets.xcconfig
GoogleService-Info.plist
*.pem
*.p8
*.p12
*.key
*.cer
*.mobileprovision
*.provisionprofile

# ── Misc ─────────────────────────────────────────────────────────────
*.playground/timeline.xctimeline
*.playground/playground.xcworkspace
Carthage/Build/
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
iOSInjectionProject/
.idea/
*.moved-aside

# Audition tool DEBUG-only SoundFonts — too large for git
# (>100 MB GitHub limit). Engineers re-download into AuditionAssets/
# per docs/superpowers/specs/2026-04-25-audition-song-playback-design.md
SurVibe/Diagnostics/AuditionAssets/*.sf2

# Production multi-channel SF2 — too large for git; placed manually per
# Packages/SVAudio/Sources/SVAudio/Resources/README.md
Packages/SVAudio/Sources/SVAudio/Resources/MuseScore_General.sf2
